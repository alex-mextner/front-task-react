# Comma-separated hosts allowed by the Vite dev server.
# Needed when accessing dev from a non-localhost address (Tailscale, LAN, ngrok).
# Without it, Vite uses its default same-origin restriction.
VITE_DEV_ALLOWED_HOSTS=
